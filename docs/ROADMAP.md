# 大创下一步

1. **视觉与坐标标定**：固定相机，标定相机坐标到机械臂基座；识别螺丝、螺孔并测量误差。
2. **闭环打螺丝原语**：实现对孔、下压、旋转、失败检测及急停；先单孔，再多孔。
3. **数据集**：记录图像、语言目标、关节状态、动作、成功/失败标签，分离训练与验证场景。
4. **VLA 接入**：用视觉语言模型给出目标/阶段，由安全的传统控制器执行，不直接把原始模型输出写入电机。
5. **评估**：统计定位误差、单孔成功率、总耗时、失败恢复率与新场景泛化；保存可复现配置和视频。

目前仓库只完成 SO-101 XYZ 控制底座，以上均为待开发任务。
